import localFont from 'next/font/local'

// define a custom local font where GreatVibes-Regular.ttf is stored in the styles folder
const GrifterBold = localFont({ src: './GrifterBold.otf' });

export { GrifterBold }